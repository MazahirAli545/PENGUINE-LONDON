export const APP_CONFIG = {
  appName: 'PENGUIN LONDON',
  version: '0.0.1',
  defaultLocale: 'en',
  apiTimeout: 30000,
  debounceSearchMs: 300,
  pagination: {
    defaultPageSize: 20,
    maxPageSize: 100,
  },
} as const;
