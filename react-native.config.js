/**
 * React Native CLI config. Assets (e.g. fonts) are linked when you run:
 * npx react-native-asset
 * (Install react-native-asset if needed: npm i -D react-native-asset)
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets/fonts'],
};
